specVersion: "v1beta"
serviceId: "bachillo"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bachillo-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
